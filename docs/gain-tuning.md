# ゲイン調整メモ

`PatrolGains` のチューニング指針。以下の数値はその結果に基づく。プラントを再同定したら「再検証チェックリスト」を回すこと。

## プラントの要点

軸ごとに「静的な指令→速度マップ ＋ 純むだ時間 ＋ 積分器」。慣性・速度ランプ・衝突・
デコードノイズは**モデルに含まれない**。つまり sim の「オーバーシュート0」「タイトな
tol 達成」は実機保証ではない。実機ステップ応答にランプが見えたら一次遅れの追加が必要。

| 軸      | 最大速度(\|cmd\|=1) | 不感帯オンセット | むだ時間 | 備考                                                          |
| ------- | ------------------- | ---------------- | -------- | ------------------------------------------------------------- |
| yaw     | ≈±199 deg/s         | 0.50             | 39 ms    | 不感帯より上はほぼ線形(≈398 deg/s per cmd)                    |
| pitch   | −237 / +181 deg/s   | 0.10             | 23 ms    | **非対称**。下向きが3割速い → −方向スルーで小オーバーシュート |
| forward | ≈±6 m/s             | 0.10             | 30 ms    | speed=0.9 で ≈5.3 m/s                                         |
| strafe  | ≈±3 m/s             | 0.10             | 29 ms    | forward の約半分                                              |

- フレーム周期 dt ≈ 17 ms(15/17.6 ms の二峰性、稀に 0.18 s スパイク)。
  **dt ジッタ/スパイクは全フェーズで実測影響ほぼゼロ**(dt キャップ 0.2 s が有効)。
- 静特性カーブは同定時に 3 点メディアン(+等間隔グリッドの端点外挿補正)で
  孤立外れ点だけ均される。単調性は課さない(カーブは順方向補間にしか
  使わないので不要)。補正が最大レートの 10% を超えた点があると品質警告が出る。
  注意: 3 点メディアンは孤立外れ値専用 — **隣接 2 レベル連続の異常は除去も
  検知もできない**ので、プロットの目視確認は省略しないこと。

## ゲイン既定値と検証済み安定範囲

数値は「既定値(検証済み安定範囲)」。範囲外の挙動は境界の性質として併記。

### face(正対) — 最もマージンが薄いフェーズ

| ゲイン        | 既定  | 安定範囲          | 境界の性質                                                                                                         |
| ------------- | ----- | ----------------- | ------------------------------------------------------------------------------------------------------------------ |
| turn_kp       | 0.05  | 0.02–0.10         | ≥0.07 でオーバーシュートが 4.5°+ に跳ねる。**≥0.15 でハードリミットサイクル**(未収束)                              |
| turn_ki       | 0.005 | 0–0.05            | 補償ありならほぼ中立。補償なし時のみ生命線(だが3〜10sのクロール)                                                   |
| turn_kd       | 0.0   | 0 固定            | D はむだ時間で遅れて逆効果(接近を遅らせリンギング)                                                                 |
| turn_deadzone | 0.50  | 実オンセット±0.10 | 過小補償側が危険: 実オンセット0.60で激遅、**0.70で完全失速**                                                       |
| pitch_kp      | 0.07  | 0.05–0.12         | ≥0.12 でオーバーシュート増、0.20 で osc 15。0.03 は補償なしだと失速                                                |
| pitch_ki      | 0.008 | 0–0.03            | ほぼ中立                                                                                                           |
| face_tol      | 2.0°  | ≥0.05°            | **0.05° が下限**。0.02° 以下は補償の底上げ残留振動で settle 不能 → それ以下の精度は align に任せる(現行設計の根拠) |

- **turn_kp=0.035 は頑健版**: むだ時間×2 でもオーバーシュートほぼゼロ
  (既定は 8.4°)、コストは静定+50ms。実機のむだ時間が同定値より伸びる兆候があれば
  こちらへ。
- 補償(deadzone)は必須。外すと kp≤0.05 は失速し、ストレス下で全滅。
  代償は tol 境界での小さな底上げキックと精度下限 0.05°。

### nav(経路追従)

| ゲイン            | 既定       | 安定範囲              | 境界の性質                                                                                                                                 |
| ----------------- | ---------- | --------------------- | ------------------------------------------------------------------------------------------------------------------------------------------ |
| nav_turn_kp       | 0.04       | 0.03–0.05             | 補償前提の値(旧 0.05–0.10 とは別物)。0.03 はゲイン誤差×1.5でむしろ悪化、0.05 は公称良だが osc↑                                             |
| nav_turn_ki       | 0.025      | 0–0.06                | ilim 0.5 がワインドアップを防ぐ。補償後は出口の巻き戻しも速い                                                                              |
| nav_turn_kd       | 0.002      | 0–0.004               | **0.004 は補償下で有害**(D のマイクロスパイクが底上げを跨いでフル不感帯キック→階段再導入)。0 はむだ時間×2で osc/オーバーシュートが先に出る |
| nav_turn_deadzone | 0.50       | 実オンセット±0.10     | face と同運用。**tol は入れない**(補償に不感帯を再導入し、kp≥0.05 でリミットサイクルを誘発する実測)                                        |
| nav_lookahead     | 1.2        | 0.8–2.5               | 小=追従タイト・円弧定常誤差大(L0.8で8.2°)/ 大=内切り増(L2.5で xtrack 0.41m)。急ジグザグ・狭所は 0.8                                        |
| fwd_kp            | 2.0        | 1–8(下限 0.29 ハード) | fwd_kp·arrive < 不感帯 0.10 だと到達半径の外で失速(後退なし・補償なしのため)。0.25 は 0.40m でスタック実証                                 |
| fwd_kd            | 0.05       | 0–0.2                 | 影響軽微                                                                                                                                   |
| speed / arrive    | 0.9 / 0.35 | —                     | speed↑=コーナー内切り↑(0.5→1.0 で xtrack 0.27→0.68m)。arrive↑=速いが最終点から遠い(final_gap≈½·arrive)                                     |

- 補償フロア=実オンセット一致が生命線(face と同じ弱点構造)。再同定でオンセットが
  動いたら nav_turn_deadzone を必ず追従させる。マウス視点(不感帯なし)では 0 に。
- carrot はコーナーを自然に丸めるため forward_factor の失速も併せて解消する
  (S字の fwd_dip 0.52→0.04)。真のヘアピン(>120°)だけは減速が残るが回復は速い。
- 密な階段状ポリライン(A*出力)では osc 指標が上がるが角加速度は低い
  (指標アーチファクト)。気になる場合は経路平滑化か nav_lookahead↑で低減。
- ゲイン誤差耐性は kd=0.002 の副次効果で改善(揺れ開始 ×1.5→×2.0、×1.75 まで
  実測無事)。それでも実機の yaw 応答が同定値(≈398 deg/s per cmd)より**速い**
  兆候があれば kp を下げる(危険なのは速い側)。

### hold-view 並進(move_to) / align(最終照準)

| ゲイン          | 既定   | 安定範囲    | 境界の性質                                                                                                                                 |
| --------------- | ------ | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------ |
| hmove_kp        | 1.0    | 0.5–2.5     | 失速下限は**方向依存**: 軸沿い 0.29(=0.10/arrive)、斜め45°は ≈0.40(誤差が両体軸に分割されるため)。発振開始 kp≈3(到達はする)、kp=8 でも到達 |
| hmove_kd        | 0.1    | 0–0.1       | **0.3 は有害**(dt スパイクとの D キックで失速)                                                                                             |
| strafe_kp       | 4.0    | 1–8         | kp=8 で軽微振動、**kp=12 でリミットサイクル**(タイムアウト)                                                                                |
| strafe_ki       | 0.8    | 0–2.0       | ilim 0.3＋条件付き積分で破綻しない                                                                                                         |
| strafe_deadzone | 0.10   | 必須        | 純Pだと kp·align_tol=0.08 < 0.10 で失速。補償なしでも ki が救うが**7倍遅い**(0.15s→1.08s) — 実機ノイズ下では失敗しうる                     |
| align_tol       | 0.02 m | ≥0.005(sim) | sim では 0.005m まで 10/10。実機の量子化ノイズで下限は上がる想定。0.02 は保守的で妥当                                                      |

- スタック検出は窓内の**経路長**(Σ|Δpos|)基準(2026-07-15 修正)。真の失速は従来通り
  約2sで検出。発振は stuck 扱いにならず align_timeout(8s)まで走る — 収束不能ケースの
  検出が最大数秒遅くなるが誤分類はしない。頻発するなら align_timeout 短縮で緩和。

## 理論クイックリファレンス(ゲインを動かすときの見積り)

小信号ループゲイン K ≈ 補償係数 × 不感帯より上のカーブ傾き。交差周波数
ωc ≈ kp·K、位相余裕 PM ≈ 90° − ωc·(むだ時間+dt)·57.3。**kp を上げると ωc が比例して
上がり、むだ時間余裕が反比例で減る** — これが全フェーズ共通の劣化メカニズム。

| ループ               | ωc          | PM        | 追加むだ時間余裕                          |
| -------------------- | ----------- | --------- | ----------------------------------------- |
| face yaw             | ≈10 rad/s   | 57°       | +100 ms(合計 ≈157 ms = 公称の約2.8倍)     |
| nav yaw(補償後)      | ≈8.0        | 64°       | +140 ms(sim 実測もむだ時間×3.5まで崖なし) |
| face pitch(−側/＋側) | 16.6 / 12.7 | 52° / 60° | +54 / +83 ms                              |
| nav forward          | 14          | 71°       | +88 ms                                    |
| hmove(kp=1)          | 3.5–8.9     | >100°     | +210 ms 以上(非常に保守的)                |
| align strafe         | 12.5        | 74°       | +103 ms                                   |

ki の位相遅れは全ループで無視できる規模(ki/kp ≪ ωc)。ki は安定性でなく
定常偏差・不感帯突破の道具として扱う。

## ロバスト性エンベロープ(既定ゲイン、実測)

| 摂動                     | face                 | nav                                      | align         | エンドツーエンド visit          |
| ------------------------ | -------------------- | ---------------------------------------- | ------------- | ------------------------------- |
| むだ時間倍率             | **≤3.25×**(3.5×でLC) | >4×                                      | >4×           | >4×(align が face の失敗を救済) |
| プラントゲイン誤差       | 0.5–2.0×             | 0.5–2.0×(≥2.0×で揺れ。×1.75まで実測無事) | 2.0×で1回失敗 | 0.5–1.5× 盤石                   |
| yaw 不感帯オンセットずれ | 補償±0.10            | 補償±0.10(2026-07-16〜)                  | —             | face に準ずる                   |
| dt ジッタ/スパイク       | 影響なし             | 影響なし                                 | 影響なし      | 影響なし                        |

**弱点は face yaw**。実機で watch すべきは (1) むだ時間の増加(×3 が崖)、
(2) yaw 不感帯の過小推定(実オンセット ≥0.60 で劣化開始)。「遅い側」のゲイン誤差
(実機がモデルより遅い)は失敗しない — 危険なのは速い側。

## チューニング手順

### 実行環境

sim の回し方・ストレス手法・指標の定義と基準値は [verification.md](verification.md)。要点:

- 全ループはヘッドレス実行可: `SimulatedVRChat`(app/sysid/simplant.py)が
  reader/look/move の3役、`SimClock` を clock に渡すと仮想時間で高速に回る。
  呼び出しパターンは tests/test_sysid.py と app/cli/sim_face.py が実例。
- **1試行ごとに sim とコントローラを必ず作り直す**(両方状態を持つ)。
  **複数フェーズ連結時は SimClock を共有する**(作り直すと見かけの巨大 dt で
  align の stuck 検出が誤発火する)。
- むだ時間ストレスは読込済みモデルの deadtime_s をスケール、ゲイン誤差ストレスは
  points の rate 側をスケール(メモリ上で。JSON は書き換えない)。
- 指標は AxisMetrics(telemetry.py)。osc(符号反転回数)が振動の主指標で 2–3 以下が
  目安・2桁は発振。settle_time=None は未収束。同成績なら effort が小さい方。
  注意: 複数ウェイポイント経路では yaw_over/peak に目標側変わりが混ざる(終端
  オーバーシュートではない)。box 経路の並進 overshoot も脚長のアーチファクト。

### 探索の作法

1. 1変数ずつ。順序は deadzone → kp → ki → kd(事故が少ない)。
2. 各候補は小誤差(2–5°/短距離)と大誤差(30–179°/長距離)の両方で評価。
   小誤差は tol 境界の挙動(補償キック)、大誤差は引き込みと飽和からの復帰を見る。
3. 生き残った候補に**むだ時間×1.5〜×2 ストレス**を必ずかける。名目性能が同等なら
   ストレス耐性が高い方を採る(kp が低い側が勝つのが通例)。
4. 発振判定は「osc が増える」より「収束するか」を優先。不感帯＋飽和のおかげで
   線形不安定でも振幅有界のまま到達するケースがある(nav yaw、hmove kp=8)。
   それは「マージンを食い潰して偶然立っている」状態なので採用しない。
5. 受け入れ基準の目安: 全対象シナリオ収束 100%、osc≤3、オーバーシュートが
   tol 比で小さい、×1.5 ストレスで劣化が連続的(崖がない)。

### プラント再同定後の再検証チェックリスト

1. 同定/読込時にメディアン補正警告やセグメント棄却警告が出ないか
   (出たらプローブデータの品質を先に疑う)。
2. 不感帯オンセットを読み直し、turn/pitch/strafe/nav_turn_deadzone をオンセット
   ちょうどに更新。**下回る設定は失速側で危険、上回るのは±0.10まで許容**。
3. fwd_kp·arrive > 新不感帯、hmove_kp·arrive > 新不感帯·√2 を確認(失速下限)。
4. face yaw の小誤差(2–5°)収束と、むだ時間×2 での収束を確認。
5. 各軸の最大速度が大きく変わったら kp を反比例で当たりを付けてから微調整
   (ωc ≈ kp·K を一定に保つ)。

### 実機答え合わせ

sim-face / patrol-buttons はゲインフラグ共通・指標も同一計算。実機で face の
yaw_err が tol 前後で往復し turn が ±0.50 を行き来していたら face yaw の振動。
turn_p/turn_i/turn_d の内訳でどの項が暴れているか分かる。**sim と実機で食い違ったら
ゲインでなくモデルを疑う**(静特性の再プロット、むだ時間、そして sim に無い
慣性・ノイズの有無)。look をマウス入力にする場合は不感帯が無いので deadzone 0、
ゲインは別物として再調整。
